.span-24.prepend-top
  - form_for @group, :html => { :multipart => true } do |f|
    .span-20.prepend-1.append-3.last
      %h2.prepend-1 New Group
      .prepend-1= f.error_messages 
      .form_row.prepend-top
        = f.label :name
        = f.text_field :name, :size => 20
      .form_row.prepend-top
        = f.label :purpose
        = f.text_field :purpose, :size => 50
      .form_row.prepend-top
        = f.label :dues
        = f.text_field :dues, :size => 3
      .form_row.prepend-top
        = f.label :photo
        = f.file_field :photo
      .form_row.prepend-top
        .prepend-4= f.submit 'Preview'
      .form_row= link_to 'Back', groups_path

